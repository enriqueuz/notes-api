# Notes API

Before you run server.py, create database tables:
* Run python3
* import models
* models.initialize()

Endpoints:
* List notes : '/' GET
* Create note : '/' POST
* Sign Up : '/signup/' 
* Login : '/login/' POST
